"""Tool wrappers exposed to the agent SDK.

All wrappers are pre-written. All side-effecting wrappers accept `dry_run: bool`
that short-circuits the external call and returns a structured preview.
"""
